# 古诗词视频 Agent 参考实现 —— 依赖清单
# 安装: .venv\Scripts\pip install -r requirements.txt

langgraph>=0.2.60            # 图编排:主图 + 单句子图 + 条件边
langgraph-checkpoint-sqlite  # SqliteSaver:断点续传(手册 M1 验收点)
openai>=1.50                 # 调 DeepSeek(OpenAI 兼容接口),你在 01_chat.py 已会
edge-tts>=6.1                # 免费 TTS 吟诵(手册选型)
httpx>=0.27                  # 调硅基流动生图/VLM(裸 HTTP,不套 SDK,教学透明)
pillow>=10.0                 # 降级方案:水墨底图 + 书法字卡
python-dotenv>=1.0           # 从上层 .env 读 API key
